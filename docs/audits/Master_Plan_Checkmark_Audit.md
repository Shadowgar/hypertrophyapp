# Master Plan Checkmark Audit - Adaptive Coaching Rebuild

Last audited: 2026-03-16

Status legend:
- VERIFIED
- PARTIAL
- NOT VERIFIED

## Verification Matrix

| Domain | Claim | Status | Evidence |
| --- | --- | --- | --- |
| Runtime source boundary | Runtime does not parse raw PDF/XLSX | VERIFIED | `apps/api/app/routers/*.py`, `importers/xlsx_to_program.py`, `importers/reference_corpus_ingest.py` |
| Runtime guide artifact coupling | API/web runtime does not read `docs/guides/*` artifacts | VERIFIED | `apps/api/app/routers/plan.py`, `apps/web/app/settings/page.tsx`, `apps/web/lib/api.ts` |
| Runtime template catalog hygiene | Runtime list excludes legacy imported artifacts | VERIFIED | `apps/api/app/program_loader.py`, `apps/api/tests/test_program_loader_dedup.py`, `programs/archive_imports/README.md` |
| Coaching timeline UX | User can view recommendation timeline with rationale | VERIFIED | `apps/web/app/history/page.tsx`, `apps/web/tests/history.analytics.test.tsx` |
| Onboarding failure transparency | Register/login/profile failures expose backend detail text | VERIFIED | `apps/web/app/onboarding/page.tsx`, `apps/web/tests/onboarding.error.test.tsx` |
| Onboarding reset controls | Test loop supports wiping stale onboarding accounts from onboarding screen | VERIFIED | `apps/api/app/routers/auth.py`, `apps/web/app/onboarding/page.tsx`, `apps/api/tests/test_auth_password_reset.py` |
| Onboarding screenshot parity translation | External onboarding references translated into explicit field/flow checklist | PARTIAL | `docs/redesign/Onboarding_Reference_Analysis_Batch1.md`, `docs/redesign/Onboarding_Reference_Analysis_Batch2.md`, `docs/redesign/Onboarding_Reference_Analysis_Batch3.md` (branch/error edge cases pending) |
| Onboarding funnel refinement v1 | Onboarding uses intro + step-based questionnaire + account stage with progress and deterministic bootstrap handoff | VERIFIED | `apps/web/app/onboarding/page.tsx`, `apps/web/tests/onboarding.program.test.tsx`, `apps/web/tests/onboarding.error.test.tsx` |
| Onboarding answer persistence | Questionnaire answers are persisted for downstream coaching logic | VERIFIED | `apps/api/app/models.py`, `apps/api/app/routers/profile.py`, `apps/api/alembic/versions/0012_user_onboarding_answers.py`, `apps/api/tests/test_profile_schema.py` |
| Calendar history view | User can browse week/month windows, filter dates, inspect day detail with plan deltas, and compare same weekdays | VERIFIED | `apps/api/app/routers/history.py`, `apps/api/tests/test_history_calendar.py`, `apps/web/app/history/page.tsx`, `apps/web/tests/history.calendar.test.tsx` |
| Ingestion quality report | Metrics for invalid sessions/missing fields are generated | VERIFIED | `importers/ingestion_quality_report.py`, `docs/validation/ingestion_quality_report.json` |
| Full extraction policy | CI/local ingestion mode policy is documented and scripted | PARTIAL | `docs/guides/FULL_EXTRACTION_RUNBOOK.md`, `scripts/reference_ingest.sh`, `scripts/verify_guides_checksums.py` |
| Full extraction artifact quality | All guides have non-empty extracted content | NOT VERIFIED | `scripts/reference_ingest.sh local-full` currently fails on `reference/My-Favorite-Exercise-for-Each-Body-Part.pdf` |
| Canonical schema completeness | Program/catalog/rules/user-state schemas are finalized and enforced | VERIFIED | `docs/contracts/Canonical_Program_Schema.md`, `docs/redesign/Adaptive_Coaching_Redesign.md`, `apps/api/app/adaptive_schema.py`, `apps/api/tests/test_adaptive_gold_schema_contract.py`, `apps/api/tests/test_user_training_state_schema_contract.py`, `apps/api/tests/test_program_onboarding_contract.py`, `apps/api/tests/test_xlsx_to_program_v2.py`, `apps/api/tests/test_pdf_doctrine_rules_v1.py`, `apps/api/tests/test_schema_validation.py`, `scripts/mini_validate.sh` |
| Gold sample baseline | One manually validated sample template + rules exists; Phase 1 program is the active administered gold sample | VERIFIED | `programs/gold/adaptive_full_body_gold_v0_1.json`, `programs/gold/pure_bodybuilding_phase_1_full_body.json`, `docs/rules/gold/adaptive_full_body_gold_v0_1.rules.json` |
| Real Phase 1 workbook import fidelity | Source pipeline can parse the real workbook into 10 weeks / 5 authored days with workbook-backed exercise ids | VERIFIED | `importers/xlsx_to_program.py`, `importers/xlsx_to_canonical_v2.py`, `importers/xlsx_to_program_v2.py`, `apps/api/tests/test_xlsx_to_onboarding_v2.py`, `apps/api/tests/test_xlsx_to_program_v2.py` |
| Adaptation preview API | Deterministic frequency adaptation supports 2/3/4/5 day targets | VERIFIED | `apps/api/app/routers/plan.py`, `apps/api/tests/test_program_frequency_adaptation_api.py`, `packages/core-engine/tests/test_onboarding_adaptation.py` |
| Adaptation apply runtime integration | Temporary frequency reductions are applied by generation path with deterministic countdown | VERIFIED | `apps/api/app/routers/plan.py`, `apps/api/alembic/versions/0011_user_active_frequency_adaptation.py`, `apps/api/tests/test_program_frequency_adaptation_api.py`, `apps/web/app/settings/page.tsx` |
| Weak-area persistence | User weak areas are stored and returned via profile API | VERIFIED | `apps/api/app/models.py`, `apps/api/app/routers/profile.py`, `apps/api/alembic/versions/0010_user_weak_areas.py` |
| Decision engine v2 | Deterministic adaptation fully grounded in canonical rules layer | PARTIAL | `packages/core-engine/core_engine/rules_runtime.py` now resolves canonical starting-load, fatigue/deload, equipment-mismatch substitution, and repeat-failure substitution runtime; `packages/core-engine/core_engine/intelligence.py` consumes the shared runtime for progression and phase-transition decisions, prefers canonical `UserTrainingState` inputs for program recommendation context, now owns weekly-review summary preparation from raw plan/log rows, now also owns workout-summary runtime preparation from raw workout-log rows plus bulk progression-state rows, now shapes repeat-failure substitution payloads for both `log-set` and `workout/today`, now normalizes planned-exercise log-set context before progression and feedback interpreters run, now prepares default workout session-state payloads around in-session reduction before the router persists them, now normalizes generate-week weekly-review overlay payloads (`adjustments`/`review_context`) through `prepare_generated_week_review_overlay`, now carries frequency-adaptation runtime context provenance through `request_runtime_trace` in preview/apply decision traces, now carries both coach-preview request-shaping and template-selection provenance through `request_runtime_trace`/`template_runtime_trace` in coach-preview decision traces, now owns deterministic coach-preview recommendation record-field payload shaping through `build_coach_preview_recommendation_record_fields`, now normalizes apply-route source recommendation payload/field preparation through `prepare_coaching_apply_runtime_source`, now owns weekly-review submit week-window shaping through `prepare_weekly_review_submit_window`, now owns coaching recommendation timeline limit/payload normalization through `normalize_coaching_recommendation_timeline_limit` and `build_coaching_recommendation_timeline_payload`, now owns profile response payload fallback/default shaping through `build_profile_response_payload`, now owns workout plan-context payload normalization and session/exercise/program resolution through `resolve_workout_plan_context`, now owns workout-today latest-plan payload selection/normalization through `resolve_workout_today_plan_payload`, and now owns workout-today plan/log runtime normalization through `build_workout_today_plan_runtime` and `build_workout_today_log_runtime` plus workout-summary and workout-today progression lookup ID normalization through `build_workout_summary_progression_lookup_runtime` and `build_workout_today_progression_lookup_runtime`, weekly-review cycle persistence shaping through `build_weekly_review_cycle_persistence_payload`, soreness create/update persistence shaping through `build_soreness_entry_persistence_payload`, body-measurement create/update persistence shaping through `build_body_measurement_create_payload`/`build_body_measurement_update_payload`, profile recovery-window runtime shaping through `prepare_profile_date_window_runtime`, profile upsert payload shaping through `build_profile_upsert_persistence_payload`, profile recommendation/switch fallback input shaping through `prepare_profile_program_recommendation_inputs`, weekly-checkin persistence/response shaping through `build_weekly_checkin_persistence_payload`/`build_weekly_checkin_response_payload`, weekly-review user update payload shaping through `build_weekly_review_user_update_payload`, weekly-review log window timestamp shaping through `prepare_weekly_review_log_window_runtime`, and workout log-set request runtime shaping through `prepare_workout_log_set_request_runtime`; `packages/core-engine/core_engine/generation.py` now also centralizes frequency-adaptation preview/apply runtime input shaping through `prepare_frequency_adaptation_runtime_inputs`, coach-preview request day normalization through `prepare_coach_preview_runtime_inputs`, guide summary resolution through `resolve_program_guide_summary`, coach-preview program-name resolution through `resolve_program_display_name`, optional rule-set loading through `resolve_optional_rule_set`, onboarding linked-program resolution through `resolve_onboarding_program_id`, and generate-week plan-call input normalization through `prepare_generate_week_plan_runtime_inputs`; `packages/core-engine/core_engine/user_state.py` now assembles canonical `UserTrainingState` from persisted runtime sources and generation-context fields, and `apps/api/app/routers/profile.py` exposes it at `GET /profile/training-state`; `apps/api/app/routers/plan.py` frequency adaptation, generate-week runtime shaping, generate-week plan-call input shaping, coach-preview history context, weekly-review overlay shaping, apply-route source normalization, guide summary lookup, coach-preview program-name lookup, optional rule-set loading, adaptation persistence shaping, onboarding program-id resolution, and recommendation timeline payload shaping now consume canonical user-state/runtime helpers through that same contract; `apps/api/app/routers/profile.py` now also consumes core-engine profile response payload shaping on `GET /profile`; `apps/api/app/routers/workout.py` now also consumes core-engine workout plan-context normalization on log-set/progress/summary routes plus workout-today latest-plan payload normalization and already consumes core-engine workout-today/workout-progress/workout-summary runtime plus optional rule-set loading; broader end-to-end rule application is still pending. |
| Library-wide migration | Remaining program library migrated to canonical v2 contracts | PARTIAL | Imported variants archived in `programs/archive_imports/`; full v2 migration of all templates still pending |

## Notes

- Current guide markdown output remains excerpt-oriented and provenance-focused.
- Redesign target requires moving coaching runtime dependence to canonical templates + typed rule objects.
- Architecture audit matrix published at `docs/redesign/Architecture_Audit_Matrix.md`.
- Frequency adaptation preview/apply now emit stricter structured traces (`version`, ordered `steps`, machine-readable `reason_code`) and consume canonical weak-area bonus-slot rules in runtime overlay/scoring paths.
- Doctrine distillation now extracts explicit scheduled-deload cadence hints (`deload every N weeks`) and repeat-failure substitution thresholds (`after N failed exposures`) into typed canonical rule payloads when source text provides them.
- Doctrine distillation now also extracts explicit fatigue-threshold trigger text (`session RPE avg >= X for N exposures`) into typed fatigue trigger condition strings when source text provides it.
- Doctrine distillation fatigue-trigger provenance now prefers explicit fatigue-threshold excerpts in `source_sections` when source text provides them.
- Decision-family decomposition has started by moving frequency-adaptation preview/apply + persistence payload builders into `core_engine/decision_frequency_adaptation.py`, reducing `intelligence.py` ownership for that family without changing router contracts.
- Frequency-adaptation decision-family decomposition now also includes active adaptation runtime resolution/application helpers (`resolve_active_frequency_adaptation_runtime`, `apply_active_frequency_adaptation_runtime`) in `core_engine/decision_frequency_adaptation.py`.
- Decision-family decomposition now also covers weekly-review utility boundaries via `core_engine/decision_weekly_review.py` (window/status/submit/persistence helpers), further reducing `intelligence.py` ownership without changing router contracts.
- Weekly-review decision-family decomposition now also covers decision interpretation/payload shaping (`interpret_weekly_review_decision`, `build_weekly_review_decision_payload`) in `core_engine/decision_weekly_review.py`.
- Weekly-review helper internals duplicated in `intelligence.py` were removed after delegation, reducing drift risk and reinforcing decision-family ownership.
- Weekly-review plan-adjustment application (`apply_weekly_review_adjustments_to_plan`) now also resides in `core_engine/decision_weekly_review.py`, further shrinking weekly-review ownership in `intelligence.py`.
- Weekly-review public exports now align with that boundary (`__init__.py` sources weekly-review plan-adjustment helpers from `decision_weekly_review.py`).
- Coach-preview payload-shaping decomposition has started via `core_engine/decision_coach_preview.py` (`build_coach_preview_payloads`, `build_coach_preview_recommendation_record_fields`) with aligned package exports.
- Coach-apply source/runtime prep internals are now also decomposed into `core_engine/decision_coach_preview.py`, with stable `intelligence.py` wrappers retained for router-facing call signatures.
- Coach-apply decision interpreters and applied-record payload builders (`interpret_coach_phase_apply_decision`, `interpret_coach_specialization_apply_decision`, `build_phase_applied_recommendation_record`, `build_specialization_applied_recommendation_record`) are now also decomposed into `core_engine/decision_coach_preview.py`, with stable `intelligence.py` wrappers retained for router-facing signatures.
- Coach-apply finalization payload shaping (`finalize_applied_coaching_recommendation`) is now also decomposed into `core_engine/decision_coach_preview.py`, with stable `intelligence.py` wrapper entrypoint retained for router-facing signatures.
- Coach-preview recommendation assembly internals (schedule/progression payload shaping, readiness fallback, decision-trace packaging, and `recommend_coach_intelligence_preview`) are now also decomposed into `core_engine/decision_coach_preview.py`, with `intelligence.py` retaining a stable wrapper that injects existing decision dependencies.
- Coaching recommendation timeline payload normalization (rationale resolution, focus-muscle extraction, timeline entry/payload builders, and timeline limit normalization) is now also decomposed into `core_engine/decision_coach_preview.py`, with stable `intelligence.py` wrappers retained for router-facing signatures.
- Shared plan/profile training-state assembly defaults are now also decomposed into `core_engine/user_state.py` via `build_plan_decision_training_state`, with `apps/api/app/routers/plan.py` and `apps/api/app/routers/profile.py` reusing the canonical helper instead of route-local payload shaping.
- Apply response finalization wiring for coach apply endpoints is now also decomposed into `core_engine/decision_coach_preview.py` via `build_applied_coaching_recommendation_response`, with `apps/api/app/routers/plan.py` reusing the shared helper instead of route-local finalize wiring.
- Applied recommendation record constructor payload shaping for coach apply endpoints is now also decomposed into `core_engine/decision_coach_preview.py` via `build_applied_coaching_recommendation_record_values`, with `apps/api/app/routers/plan.py` reusing the shared helper instead of route-local field wiring.
- Coach-preview runtime context assembly is now also decomposed into `core_engine/decision_coach_preview.py` via `prepare_coach_preview_decision_context`, with `apps/api/app/routers/plan.py` reusing the shared helper instead of route-local training-state/context shaping.
- Generate-week canonical runtime assembly is now further centralized in `core_engine/generation.py` via `prepare_plan_generation_decision_runtime`, so `apps/api/app/routers/plan.py::_prepare_plan_generation_runtime` keeps SQL fan-in while deterministic training-state/runtime payload shaping is interpreter-owned.
- Frequency-adaptation generation runtime contract now preserves top-level `context_trace` in `prepare_frequency_adaptation_decision_runtime`, maintaining stable trace provenance for downstream decision families and focused generation tests.
- Weekly-review performance summary computation is now also decomposed into `core_engine/decision_weekly_review.py` via `summarize_weekly_review_performance` and `build_weekly_review_performance_summary`, and package exports now source those helpers from the decision-family module.
- Duplicate weekly-review summary helper internals were removed from `core_engine/intelligence.py` after delegation, reducing split-brain risk for weekly-review fault-scoring and summary trace logic.
- Coach-apply confirm-path commit orchestration is now also decomposed into `core_engine/decision_coach_preview.py` via `prepare_applied_coaching_recommendation_commit_runtime` and `finalize_applied_coaching_recommendation_commit_runtime`, and `apps/api/app/routers/plan.py` now reuses the shared helper flow for both phase and specialization apply paths.
- Coach-apply decision-request branch wiring is now also decomposed into `core_engine/decision_coach_preview.py` via `prepare_coaching_apply_decision_runtime`, and `apps/api/app/routers/plan.py` now reuses that shared helper instead of duplicating source-runtime unpacking across phase/specialization apply paths.
- Coach-apply commit payload-key mapping is now also decomposed into `core_engine/decision_coach_preview.py` via `prepare_coaching_apply_commit_runtime`, and `apps/api/app/routers/plan.py` now reuses that shared helper instead of route-local phase/specialization payload-key branching.
- Coach-apply route preflight/confirm branching runtime is now also decomposed into `core_engine/decision_coach_preview.py` via `prepare_coaching_apply_route_runtime`, and `apps/api/app/routers/plan.py` now reuses that shared helper instead of route-local branching glue.
- Weekly-review submit constructor payload wiring is now also decomposed into `core_engine/decision_weekly_review.py` via `prepare_weekly_review_submit_persistence_values`, and `apps/api/app/routers/profile.py` now reuses that helper instead of route-local checkin/review field mapping.
- Coach-preview record commit wiring is now also decomposed into `core_engine/decision_coach_preview.py` via `prepare_coach_preview_commit_runtime` and `finalize_coach_preview_commit_runtime`, and `apps/api/app/routers/plan.py` now reuses those helpers instead of route-local pre/post-flush payload wiring.
- Workout log-set exercise-state progression runtime wiring is now further centralized in `core_engine/intelligence.py` via `prepare_workout_exercise_state_runtime`, and `apps/api/app/routers/workout.py` now reuses that helper instead of route-local first-exposure start-load + progression-state field mapping.
- Workout log-set `ExerciseState` persistence payload shaping is now further centralized in `core_engine/intelligence.py` via `prepare_workout_log_set_decision_runtime` (`exercise_state_create_values` and `exercise_state_update_values`), and `apps/api/app/routers/workout.py` now reuses those payloads instead of route-local state field mapping.
- Frequency-adaptation preview/apply deterministic route orchestration is now further centralized in `core_engine/decision_frequency_adaptation.py` via `prepare_frequency_adaptation_route_runtime`, and `apps/api/app/routers/plan.py` now reuses that helper for shared preview/apply runtime argument wiring and apply payload shaping while keeping SQL and persistence in-router.
- Coach-preview deterministic route composition is now further centralized in `core_engine/decision_coach_preview.py` via `prepare_coach_preview_route_runtime`, and `apps/api/app/routers/plan.py` now reuses that helper for preview interpreter dispatch + commit-runtime preparation while keeping SQL and persistence in-router.
- Profile recommendation deterministic route composition is now further centralized in `core_engine/intelligence.py` via `prepare_profile_program_recommendation_route_runtime`, and `apps/api/app/routers/profile.py` now reuses that helper across recommendation/switch flows while keeping SQL and persistence in-router.
- Weekly-review submit deterministic route composition is now further centralized in `core_engine/decision_weekly_review.py` via `prepare_weekly_review_submit_route_runtime`, and `apps/api/app/routers/profile.py` now reuses that helper while keeping SQL reads and persistence in-router.
- Coach apply deterministic route finalization is now further centralized in `core_engine/decision_coach_preview.py` via `prepare_coaching_apply_route_finalize_runtime`, and `apps/api/app/routers/plan.py` now reuses that helper for preflight-vs-confirm final response payload handling while keeping SQL and persistence in-router.
- Workout session-state deterministic upsert payload composition is now further centralized in `core_engine/intelligence.py` via `prepare_workout_session_state_upsert_runtime`, and `apps/api/app/routers/workout.py` now reuses that helper while keeping ORM read/write in-router.
- Weekly-review status/summary deterministic route composition is now further centralized in `core_engine/decision_weekly_review.py` via `prepare_weekly_review_status_route_runtime` and `prepare_weekly_review_summary_route_runtime`, and `apps/api/app/routers/profile.py` now reuses those helpers while keeping SQL reads/writes in-router.
- `apps/api/app/routers/plan.py` apply-phase/apply-specialization route orchestration is now consolidated behind a shared private helper (`_apply_coaching_decision_route`), reducing duplicate route plumbing while keeping DB access in-router and deterministic apply logic in core-engine.
- Workout today/progress/summary deterministic route composition is now further centralized in `core_engine/decision_workout_session.py` via `prepare_workout_today_plan_route_runtime`, `prepare_workout_today_selection_route_runtime`, `prepare_workout_today_response_runtime`, `prepare_workout_progress_route_runtime`, and `prepare_workout_summary_route_runtime`, and `apps/api/app/routers/workout.py` now reuses those helpers while keeping SQL reads, optional rule-set loading, and HTTP mapping in-router.
- Generate-week deterministic route composition is now further centralized in `core_engine/generation.py` via `prepare_generate_week_scheduler_runtime`, `prepare_generate_week_review_lookup_runtime`, and `prepare_generate_week_finalize_runtime`, and `apps/api/app/routers/plan.py` now reuses those helpers while keeping SQL fan-in, review-cycle lookup, DB writes, and HTTP mapping in-router.
- The workout route family is now further completed behind `core_engine/decision_workout_session.py` via `prepare_workout_log_set_context_route_runtime`, `prepare_workout_log_set_decision_route_runtime`, `prepare_workout_session_state_route_runtime`, `prepare_workout_log_set_response_runtime`, `prepare_workout_today_progression_route_runtime`, and `prepare_workout_summary_response_runtime`, and `apps/api/app/routers/workout.py` now reuses those helpers while keeping ORM reads/writes, SQL fan-in, and HTTP mapping in-router.
- Program recommendation/switch decision-family decomposition now lives in `core_engine/decision_program_recommendation.py`, with `intelligence.py` retaining stable compatibility wrappers and `__init__.py` sourcing the public export surface from the dedicated module.
- Schedule adaptation, readiness, progression action, and phase-transition decision-family decomposition now lives in `core_engine/decision_progression.py`, with `intelligence.py` retaining stable wrappers so coach-preview and other consumers keep the existing call surface.
- `docs/current_state_decision_runtime_map.md` now serves as the operational authority map for current local-branch ownership; use it to resolve conflicts between historical handoff prose and the live module layout.
- Router glue is no longer the dominant delivery risk; current dominant risks are `intelligence.py` compatibility-hub drift, shallow first-class coaching state in `apps/api/app/models.py`, an early-stage deterministic stimulus-fatigue-response layer that is not yet broadly consumed for bounded adjustments, and documentation drift.
- Live workout guidance extraction is now complete via `core_engine/decision_live_workout_guidance.py`, and `core_engine/intelligence.py` now keeps only thin compatibility wrappers for that family.
- First-class coaching constraint state has now started to land: `apps/api/app/models.py` persists `session_time_budget_minutes`, `movement_restrictions`, and `near_failure_tolerance`; `apps/api/app/adaptive_schema.py` now models canonical `constraint_state`; `core_engine/user_state.py` threads those fields through `UserTrainingState` and plan-decision training-state helpers; and focused profile/training-state regressions are green.
- Weekly-checkin-backed readiness state has now started to land: `WeeklyCheckin` persists `sleep_quality`, `stress_level`, and `pain_flags`; canonical `UserTrainingState` now includes `readiness_state`; and focused weekly-checkin/profile-training-state regressions are green.
- Coach-preview now consumes canonical `readiness_state` inside deterministic readiness scoring when the request omits `readiness_score`, and the preview trace records that source explicitly.
- Weekly-review now also consumes canonical `readiness_state` from the latest persisted check-in, and the review trace records readiness-state penalty source plus matched rules.
- The first deterministic `stimulus_fatigue_response` snapshot now exists in the progression family and is surfaced through coach-preview traces.
- That SFR snapshot now also affects real behavior in progression/deaload gating and weekly-review guidance.
- Generated-week now consumes canonical `progression_state_per_exercise` and can auto-substitute after repeat-failure thresholds are reached, preserving `primary_exercise_id` and surfacing a structured `repeat_failure_substitution` payload on planned exercises.
- The adaptive gold runtime path now preserves onboarding exercise knowledge at the loader boundary, so gold generated-week substitutions can carry canonical substitute ids/names and movement metadata instead of dropping to raw fallback ids.
- The adaptive gold runtime path now has focused API proof for recovery-limited `early_sfr_recovery` deloads and persisted weekly-review overlay carry-forward on the next generated gold week, not just base loading and substitution behavior.
- The adaptive gold runtime path now also has focused workout API proof that generated-week substitution choices and live substitution guidance survive into `GET /workout/today` and remain coherent after `POST /workout/{workout_id}/log-set`.
- The adaptive gold runtime proof now covers both exercise families currently present in the sample, and scheduler substitution fallback preserves original movement metadata when substitute metadata is partial.
- The adaptive gold sample itself now includes a third exercise family with focused loader, generated-week, and workout continuity proof, expanding the gold-path baseline without changing the loader boundary architecture.
- The adaptive gold sample now also includes a fourth lower-body family with the same loader, generated-week, and workout continuity proof, further broadening the gold-path baseline without changing the loader boundary architecture.
- The adaptive gold sample now also includes a fifth core/ab family (`cable_crunch`) with focused loader, generated-week, and workout continuity proof, so the current gold baseline is no longer limited to presses, pulls, and lower-body compounds.
- The adaptive gold sample now also includes a sixth hinge/posterior-chain family (`romanian_deadlift`) with focused loader, generated-week, and workout continuity proof, so the current gold baseline now spans both quad-dominant and hip-hinge lower-body doctrine.
- The adaptive gold sample now also includes explicit weak-area/accessory coverage through `weak_chest_cable_fly` and `weak_ham_leg_curl`, with focused loader, generated-week, and workout continuity proof. The gold baseline now spans both general compound coverage and preserved weak-point doctrine.
- The canonical gold onboarding exercise library now includes true arm-isolation entries (`dumbbell_curl_incline`, `triceps_pushdown_rope`), and the adaptive gold sample plus focused loader/generated-week/workout proofs now cover direct biceps/triceps isolation as part of the validated baseline.
- The adaptive gold path now starts from a five-day authored source (`Full Body #1-#4` plus `Arms & Weak Points`) rather than the earlier compressed three-day surrogate, and runtime loader support now preserves explicit `day_role` metadata through the canonical template boundary.
- The real Phase 1 workbook now imports cleanly through the build-time pipeline: header detection no longer grabs workbook note blocks, Excel `m-d` serial cells decode correctly, and superset prefixes are stripped before exercise ids are slugged.
- Key workbook exercise metadata is now materially more trustworthy at the source boundary: `leg_press` resolves as squat-pattern, `seated_db_shoulder_press` resolves as vertical press, `triceps_pressdown_bar` resolves as triceps extension, and `bayesian_cable_curl` resolves as curl.
- `apps/api/app/program_loader.py` can now flatten all authored weeks from a source-backed multi-phase adaptive-gold bundle, which removes a runtime blocker for migrating the live gold artifacts toward the repaired workbook pipeline.
- Weak-point doctrine is now covered under constrained time/frequency from that five-day authored truth: focused scheduler and adaptive-gold API/workout tests prove weak-point-day preservation and primary-compound preservation under five-day-to-three-day compression, with dropped-day work merged into retained anchor sessions instead of being silently lost.
- The adaptive gold sample now also preserves authored week structure through the runtime path: loader/runtime contracts carry `authored_weeks`, generation selects week variants from `prior_generated_weeks`, and focused API tests prove the second generated week resolves to authored week 2 instead of silently repeating week 1.
- The adaptive gold sample now preserves authored mesocycle intent across a real 10-week sequence, not just a short early proof slice: authored week role metadata survives the loader boundary, scheduler marks authored deload weeks explicitly in `mesocycle`, and focused engine/API tests now prove week-2 Build-B selection, week-6 authored deload selection, and later intensification-week selection on the validated gold runtime.
- When an authored deload overlaps the generic cadence, scheduler now reports `authored_deload` as the primary deload reason instead of collapsing the doctrine-backed signal into a generic scheduled reason.
- User testing now has a concrete rollout plan for desktop and mobile browser testing, so the path from internal deterministic validation to real-user dogfooding is explicitly documented instead of implied.
- The validated gold runtime now also has explicit post-sequence behavior: once the authored 10-week mesocycle is exhausted, scheduler keeps the final authored week as the deterministic fallback while surfacing `authored_sequence_complete` and `phase_transition_pending` instead of silently clamping with no transition signal.
- That post-sequence state now survives the full deterministic path: canonical training state exposes `generation_state.latest_mesocycle`, coach-preview surfaces `transition_pending` plus `recommended_action == "rotate_program"`, and program recommendation now treats authored-sequence completion as a stronger signal than simple day-adaptation upgrades.
- Coach-preview specialization adjustment and program-media/warmup summarization now live in `decision_coach_preview.py`, reducing another mixed-owner seam inside `intelligence.py`.
- Workout performance summary and exercise-state shaping now resolve through `decision_workout_session.py` on the package surface and route-runtime path, reducing the largest remaining workout ownership ambiguity.
- Workout helper compatibility wrappers in `core_engine/intelligence.py` now preserve the legacy public surface while delegating fully to `decision_workout_session.py`, so the workout family is now façade-only in `intelligence.py`.
- Generated-week now uses persisted exercise recovery state beyond repeat-failure substitution: high `fatigue_score` plus recent `reduce_load` actions can reduce planned sets/load and elevate substitution pressure per exercise.
- Generated-week now also consumes canonical profile time budget constraints: low `session_time_budget_minutes` can deterministically cap per-session exercise count.
- Generated-week now consumes canonical movement restrictions where template `movement_pattern` metadata is present, filtering restricted movement patterns during session construction.
- Adaptive gold assets are now first-class runtime loader inputs: `apps/api/app/program_loader.py` can list the gold sample in the runtime catalog, adapt `programs/gold/adaptive_full_body_gold_v0_1.json` into the current runtime template contract, and resolve `docs/rules/gold/adaptive_full_body_gold_v0_1.rules.json` through the standard rule-loader boundary.
- The first adaptive-gold end-to-end runtime scenario is now verified at the API layer: `generate-week` -> `log-set` -> `weekly-review` -> next `generate-week` remains on the gold program and carries the saved adaptive-review overlay.
- The next implementation target is extending those bounded generation-pressure paths only where richer state exists and template metadata is deterministic enough to support it, while continuing to shrink non-workout mixed-owner seams in `intelligence.py` that block coaching-state or SFR work.
