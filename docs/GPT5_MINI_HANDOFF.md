# GPT-5-mini Handoff - Adaptive Coaching Rebuild

Last updated: 2026-03-11

## Mission

Rebuild the platform into a deterministic adaptive hypertrophy coaching system based on:
- Excel-derived structured templates
- PDF-derived explicit coaching rules

## Active Baseline Docs

- `docs/AI_CONTINUATION_GOVERNANCE.md`
- `docs/Master_Plan.md`
- `docs/redesign/Adaptive_Coaching_Redesign.md`
- `docs/GPT5_MINI_EXECUTION_BACKLOG.md`

## Current State Snapshot

Implemented and pushed:
- Coaching preview/apply timeline UX
- Ingestion quality reporting
- XLSX sanitization and preflight audit guard
- Workout router sovereignty now covers live session-state reduction, workout progress payload shaping, and workout-today response hydration via `packages/core-engine/core_engine/intelligence.py`
- Workout-today session selection policy now also delegates to `packages/core-engine/core_engine/intelligence.py`; the router keeps only the supporting SQL reads needed to evaluate resume eligibility.
- Workout stored-plan traversal now also delegates to `packages/core-engine/core_engine/intelligence.py`; the router keeps only the workout-plan SQL query before passing payloads into the engine lookup helper.
- Weekly review window/status resolution now delegates to `packages/core-engine/core_engine/decision_weekly_review.py`; `apps/api/app/routers/profile.py` keeps the weekly review SQL reads and persistence while using the engine for deterministic date-window and response-status shaping.
- Program recommendation runtime preparation and program-switch runtime preparation now delegate to `packages/core-engine/core_engine/decision_program_recommendation.py`; `packages/core-engine/core_engine/intelligence.py` keeps stable wrapper entrypoints while `apps/api/app/routers/profile.py` keeps only latest-user-state SQL reads plus confirmed selected-program persistence.
- Weekly review summary preparation, submit decision packaging, and response shaping now delegate to `packages/core-engine/core_engine/decision_weekly_review.py`; `apps/api/app/routers/profile.py` keeps only summary SQL reads plus review/check-in/user persistence.
- Workout completed-set aggregation now delegates to `packages/core-engine/core_engine/intelligence.py`; `apps/api/app/routers/workout.py` keeps workout log SQL reads while the engine computes highest completed set indexes per exercise for `today` and `progress` responses.
- Workout summary runtime preparation now delegates to `packages/core-engine/core_engine/intelligence.py`; `apps/api/app/routers/workout.py` keeps planned-session, workout-log, and bulk progression-state SQL reads before passing normalized runtime inputs into the engine.
- Workout-today session-state payload preparation, merge, and live recommendation hydration now delegate to `packages/core-engine/core_engine/intelligence.py`; `apps/api/app/routers/workout.py` keeps the session-state and progression-state SQL queries but no longer shapes per-exercise substitution payloads inline.
- Workout-today resume-state derivation now delegates to `packages/core-engine/core_engine/intelligence.py`; `apps/api/app/routers/workout.py` keeps only the workout-log SQL read before passing normalized logs and sessions into the engine.
- Workout today/progress/summary route-runtime composition now delegates to `packages/core-engine/core_engine/decision_workout_session.py`; `apps/api/app/routers/workout.py` keeps only SQL reads, optional rule-set loading, and HTTP error mapping around those helpers.
- Workout log-set context/decision/response composition and session-state route-runtime composition now also delegate to `packages/core-engine/core_engine/decision_workout_session.py`; `apps/api/app/routers/workout.py` keeps ORM reads/writes plus final HTTP response validation while the workout decision-family owns request normalization, plan/rule lookup shaping, session-state upsert payloads, and log-set response payload shaping.
- Plan guide catalog/day/exercise response shaping now delegates to `packages/core-engine/core_engine/generation.py`; `apps/api/app/routers/plan.py` keeps template loading, validation failures, and not-found handling.
- Plan template selection/runtime preparation now delegates to `packages/core-engine/core_engine/generation.py`; `apps/api/app/routers/plan.py` keeps only HTTP error translation, downstream SQL reads, and persistence around coach-preview and generate-week.
- Generate-week scheduler/finalize route composition now delegates to `packages/core-engine/core_engine/generation.py`; `apps/api/app/routers/plan.py` keeps template loading, SQL reads, review-cycle lookup, persistence, and HTTP error mapping while generation helpers own scheduler kwargs, review-week lookup parsing, and finalized plan/record payload shaping.
- Canonical gold schema contracts are stricter: `apps/api/app/adaptive_schema.py` now enforces enum validation for adaptive work-set `set_type`, optional warmup `rest_seconds`, unique adaptive phase/week/day/slot structural keys, and non-empty unique gold rule `program_scope`, with direct negative-path coverage in schema tests.
- Canonical user-training-state contracts now exist in `apps/api/app/adaptive_schema.py` via `UserProgramState`, `ExercisePerformanceEntry`, `ProgressionStateEntry`, `FatigueState`, `AdherenceState`, `StallState`, and `UserTrainingState`, with direct validation coverage in `apps/api/tests/test_user_training_state_schema_contract.py` and a green full mini-validation pass.
- Canonical user training state is now runtime-backed: `packages/core-engine/core_engine/user_state.py` assembles `UserTrainingState` from persisted plans, workout logs, progression state, soreness, check-ins, and weekly reviews, and `GET /profile/training-state` exposes that canonical payload through the API with focused engine/API regressions. The contract now also carries recommendation-facing generation context such as under-target muscles and mesocycle trigger weeks.
- Frequency adaptation preview/apply now consume canonical user training state for week-index and recovery-state resolution, and week-generation runtime input shaping now consumes canonical performance history, adherence, soreness-by-muscle, and prior-generated-week context from that same user-state payload.
- Coach-preview context assembly now also prefers canonical user training state for recent performance history, so the live preview route no longer serializes raw workout-log history directly into engine context.
- A dedicated rules-runtime entry point now exists in `packages/core-engine/core_engine/rules_runtime.py`; first-exposure starting-load resolution now applies canonical `starting_load_rules` to `POST /workout/{workout_id}/log-set`, seeds new `ExerciseState` records from estimated-1RM fallback when available, and returns an optional `starting_load_decision_trace` in the log-set response.
- Fatigue and deload trigger parsing now also lives in `packages/core-engine/core_engine/rules_runtime.py`; `packages/core-engine/core_engine/intelligence.py` now consumes rule-derived intro-week protection, fatigue RPE thresholds, early-deload trigger matching, and scheduled-deload cadence through that shared runtime instead of parsing rule payloads inline.
- Substitution rule parsing and equipment-mismatch execution now also live in `packages/core-engine/core_engine/rules_runtime.py`; `packages/core-engine/core_engine/scheduler.py` now routes compatible-substitution selection through that shared runtime during week generation, and generated exercises emit a `substitution_decision_trace` describing whether the authored exercise remained compatible or auto-swapped to the first compatible substitute.
- `substitution_rules.repeat_failure_trigger` is now operational on a real runtime path: `POST /workout/{workout_id}/log-set` updates persisted under-target streak state, `GET /workout/today` rehydrates the same stateful outcome, and live workout guidance now emits an optional `substitution_recommendation` once the canonical failed-exposure threshold is reached.
- Current local-branch authority is now documented in `docs/current_state_decision_runtime_map.md`, which records authoritative module ownership, canonical inputs/outputs, trace expectations, and whether `intelligence.py` is façade-only or still owns live logic for each decision family.
- Live workout guidance and set-feedback ownership now delegates to `packages/core-engine/core_engine/decision_live_workout_guidance.py`; `packages/core-engine/core_engine/intelligence.py` keeps compatibility wrappers while the authoritative module owns feedback interpretation, live adjustments, hydrated guidance payloads, session-state guidance updates, and overall session-guidance summaries.
- Persisted coaching constraint state has started to become first-class: `apps/api/app/models.py` now stores `session_time_budget_minutes`, `movement_restrictions`, and `near_failure_tolerance`; `apps/api/app/adaptive_schema.py` now models canonical `constraint_state`; `packages/core-engine/core_engine/user_state.py` now threads profile constraints through `UserTrainingState` and plan-decision training-state payloads; and `GET /profile` plus `GET /profile/training-state` now expose those fields through focused regressions.
- Weekly-checkin-backed readiness state is now first-class: `WeeklyCheckin` now persists `sleep_quality`, `stress_level`, and `pain_flags`; canonical `UserTrainingState` now includes `readiness_state`; and `/profile/training-state` exposes derived `recovery_risk_flags` for downstream decision families.
- Coach-preview now consumes canonical `readiness_state` when the request does not override `readiness_score`: `generation.build_coach_preview_context` carries `readiness_state`, `decision_coach_preview.py` forwards it into `decision_progression.derive_readiness_score`, and low-sleep/high-stress/pain signals can now keep a deload preview from resuming accumulation too early.
- Weekly review now also consumes canonical `readiness_state` from the latest persisted check-in when available: `apps/api/app/routers/profile.py` fans that state into `decision_weekly_review.py`, and review traces now record readiness-state penalties and source rather than relying only on nutrition/adherence.
- The first deterministic stimulus-fatigue-response layer now lives in `decision_progression.py`: progression decisions emit a derived `stimulus_fatigue_response` snapshot, and coach-preview traces now carry that snapshot end-to-end without changing router contracts.
- That SFR snapshot is no longer trace-only: progression can now deload when SFR shows both high deload pressure and low recoverability, and weekly-review guidance now uses the same snapshot to suppress push-style guidance when recovery is clearly limited.
- Generate-week now consumes canonical per-exercise progression state: `_prepare_plan_generation_runtime` fans in persisted `ExerciseState` rows, `build_plan_decision_training_state` no longer drops them, and `generation.py` carries canonical `progression_state_per_exercise` through scheduler runtime shaping.
- Repeat-failure substitution is now live on generated-week output as well as workout execution: `scheduler.py` uses `rules_runtime.resolve_repeat_failure_substitution` during planned exercise assembly, auto-swaps to the first compatible alternative when the failed-exposure threshold is met, preserves `primary_exercise_id`, and emits structured `repeat_failure_substitution` payloads on generated exercises.
- `rules_runtime.resolve_substitution_rule_runtime` now keeps the default repeat-failure threshold (`3`) even without a linked rule set so generated-week and workout paths share the same fallback doctrine.
- Specialization-adjustment and program-media/warmup helper ownership now lives in `packages/core-engine/core_engine/decision_coach_preview.py`; `intelligence.py` keeps compatibility wrappers only for those coach-preview helpers.
- Workout performance summary and exercise-state shaping ownership now resolves through `packages/core-engine/core_engine/decision_workout_session.py`: the package surface and route-runtime module now source log-set request/runtime shaping, session-state defaults/persistence helpers, workout-today plan/log/progression runtime helpers, repeat-failure substitution payload shaping, and workout-summary builders from the workout decision-family module first.
- Workout helper compatibility wrappers in `packages/core-engine/core_engine/intelligence.py` now preserve the legacy public surface while delegating fully to `decision_workout_session.py`, including session-state persistence/upsert helpers and workout-today/summary lookup runtimes.
- Generated-week now applies bounded exercise-level recovery pressure from persisted `progression_state_per_exercise`: high exercise fatigue plus recent `reduce_load` actions can reduce planned sets/load and elevate substitution pressure without waiting for a global deload.
- Generated-week now also respects canonical `constraint_state.session_time_budget_minutes`: low time budgets can cap per-session exercise count after session construction, using the persisted profile constraint rather than new request-time heuristics.
- Generated-week now respects canonical `constraint_state.movement_restrictions` where templates expose deterministic `movement_pattern` metadata: restricted movement patterns are filtered out during session construction rather than being left to ad hoc router or UI logic.
- Adaptive gold assets are now first-class runtime loader inputs: `apps/api/app/program_loader.py` can now list `programs/gold/adaptive_full_body_gold_v0_1.json` in the runtime catalog, adapt it into the current runtime template contract, and resolve `docs/rules/gold/adaptive_full_body_gold_v0_1.rules.json` through the same rule-loader boundary used by generate-week and workout flows.
- The first persisted adaptive-gold runtime scenario is now covered end to end: one focused API test now proves `generate-week` -> `log-set` -> `weekly-review` -> next `generate-week` on `adaptive_full_body_gold_v0_1`, with the second generated week carrying the saved adaptive-review overlay while keeping the gold program selected.
- The adaptive-gold runtime path now also proves doctrine-backed repeat-failure substitution: `apps/api/app/program_loader.py` hydrates movement/equipment/substitution metadata from `programs/gold/pure_bodybuilding_phase_1_full_body.onboarding.json`, and generated-week can now auto-substitute `bench_press_barbell` -> `Incline Dumbbell Press` on the gold path when persisted failed-exposure state reaches the rule threshold.
- The adaptive-gold runtime proof now also covers recovery-limited deload and next-week overlay carry-forward: focused API tests verify that canonical `readiness_state` can trigger `early_sfr_recovery` on `adaptive_full_body_gold_v0_1` and that a persisted weekly-review adjustment record still shapes the next generated gold week through `adaptive_review`.
- The adaptive-gold workout path now also has continuity proof: focused workout API tests verify that `GET /workout/today` reflects generated-week repeat-failure substitutions on the gold program and that gold `log-set` substitution guidance persists into the next `today` payload through hydrated live recommendations.
- The adaptive-gold proof is no longer bench-only: the second sample exercise family now also has generated-week and workout-side substitution coverage, and scheduler fallback logic now preserves original movement metadata when a substitute has only partial onboarding metadata.
- The authored adaptive-gold sample itself is now broader: `programs/gold/adaptive_full_body_gold_v0_1.json` includes a third vertical-pull slot, and the gold-path proof now covers generated-week plus workout continuity for that third family as well.
- The authored adaptive-gold sample now also includes a fourth lower-body slot, and the gold-path proof covers that lower-body family through generated-week plus workout continuity as well.
- The authored adaptive-gold sample now also includes a fifth core/ab slot (`cable_crunch`), and focused loader/generate-week/workout tests now verify that the core slot survives the runtime path when cable equipment is available.
- The authored adaptive-gold sample now also includes a sixth hinge/posterior-chain slot (`romanian_deadlift`), and focused loader/generate-week/workout tests now verify that the hinge slot survives the runtime path when barbell/dumbbell equipment is available.
- The authored adaptive-gold sample now also includes onboarding-backed accessory/weak-point slots for chest and hamstrings (`weak_chest_cable_fly`, `weak_ham_leg_curl`), and focused loader/generate-week/workout tests verify that those weak-area slots survive the runtime path when cable/machine equipment is available.
- The prior canonical arm-isolation gap on the gold path is now closed: `programs/gold/pure_bodybuilding_phase_1_full_body.onboarding.json` includes `dumbbell_curl_incline` and `triceps_pushdown_rope` in the exercise library, and the adaptive-gold sample plus focused loader/generate-week/workout tests now prove those biceps/triceps slots survive the runtime path with canonical metadata.
- The adaptive-gold sample is now authored as a real three-day week instead of one oversized session: `programs/gold/adaptive_full_body_gold_v0_1.json` now defines `Full Body A/B/C`, `apps/api/app/program_loader.py` derives adaptive-gold `days_supported` from authored day count, and focused loader tests verify that the runtime contract survives the multi-day shape.
- Generated-week now preserves weak-point structure under bounded compression: `packages/core-engine/core_engine/scheduler.py` now carries `slot_role`, prioritizes weak-point/accessory retention during time-budget capping, and uses weak-point structural priority as the fallback when frequency compression has no history-driven ranking to reuse.
- Focused adaptive-gold API tests now prove two multi-day doctrine constraints: weak-point days survive three-day-to-two-day frequency compression, and weak-point slots survive low time-budget capping on the selected session through bounded removal rather than naive authored-order trimming.
- Focused adaptive-gold workout tests now prove the correct multi-day `GET /workout/today` contract: when no session matches the current date, workout-today falls back to the first authored day only, preserving Day A weak-point/core slots while excluding later-day hinge and arm-isolation slots.
- Adaptive-gold is now a real 10-week authored mesocycle as well as a multi-day week: `programs/gold/adaptive_full_body_gold_v0_1.json` now follows the onboarding-backed sequence `build_a / build_b / build_a / build_b / build_a / deload / intens_a / intens_b / intens_a / intens_b`.
- `apps/api/app/program_loader.py` still preserves `authored_weeks` in the canonical runtime template, and `packages/core-engine/core_engine/scheduler.py` still selects the authored week from `prior_generated_weeks` before applying compression, capping, substitution, and SFR logic.
- Authored week roles are now explicit across the full adaptive-gold mesocycle: weeks 1-5 are `accumulation`, week 6 is `deload`, and weeks 7-10 are `intensification`.
- Focused loader/API tests now prove the later-week contract instead of only the early proof slice: week 2 selects the Build-B bench bracket (`6-9`), week 6 resolves to the authored deload with `mesocycle.deload_reason == "authored_deload"`, and weeks 8 and 10 resolve to later intensification brackets (`4-6`) on the gold runtime path.
- Post-week-10 behavior on the gold path is now explicit instead of silent: `packages/core-engine/core_engine/scheduler.py` now marks `mesocycle.authored_sequence_complete`, `phase_transition_pending`, `phase_transition_reason == "authored_sequence_complete"`, and `post_authored_behavior == "hold_last_authored_week"` when generation moves beyond the final authored week while still holding the last authored week as the deterministic fallback workout doctrine.
- Focused engine/API tests now prove that post-week-10 state: the gold path still serves week-10 intensification exercise targets while surfacing explicit transition-pending state instead of silently clamping with no signal.
- That post-week-10 state now propagates beyond scheduler output: `packages/core-engine/core_engine/user_state.py` preserves it in canonical `generation_state.latest_mesocycle`, `packages/core-engine/core_engine/decision_coach_preview.py` surfaces it in preview payloads as `phase_transition.transition_pending` plus `recommended_action == "rotate_program"`, and `packages/core-engine/core_engine/decision_program_recommendation.py` now prioritizes rotation when the authored sequence is already complete.
- Focused engine/API regressions now prove the whole handoff path instead of only scheduler output: `packages/core-engine/tests/test_decision_progression.py`, `packages/core-engine/tests/test_decision_coach_preview.py`, `packages/core-engine/tests/test_decision_program_recommendation.py`, `packages/core-engine/tests/test_user_state.py`, `apps/api/tests/test_plan_intelligence_api.py`, and `apps/api/tests/test_program_recommendation_and_switch.py`.
- User testing now has an explicit rollout plan in `docs/plans/2026-03-11-user-testing-rollout-plan.md`; the intended first real-user path is the responsive web product on desktop and mobile browsers, not a native app branch.
- Router glue is no longer the dominant risk. The branch is now constrained mainly by authority ambiguity in `intelligence.py`, shallow first-class coaching state, threshold-heavy progression logic, and documentation drift.
- The next high-value seam is no longer “what happens after week 10?” in the engine. It is making that stable transition state useful to real testers:
  - surface the new transition-pending guidance cleanly in the web UI
  - add a lightweight tester runbook / issue template
  - then broaden canonical exercise/library coverage from `/reference` instead of hand-expanding only the adaptive-gold sample

Started for redesign foundation:
- Gold sample template: `programs/gold/adaptive_full_body_gold_v0_1.json`
- Gold sample rules: `docs/rules/gold/adaptive_full_body_gold_v0_1.rules.json`
- Redesign authority doc: `docs/redesign/Adaptive_Coaching_Redesign.md`

## Immediate Next Execution

1. Use `docs/current_state_decision_runtime_map.md` as the cleanup contract for all further decision-runtime work.
2. Keep `packages/core-engine/core_engine/decision_live_workout_guidance.py` authoritative and do not add new workout-guidance logic back into `intelligence.py`.
3. The workout family is now facade-only in `intelligence.py`; do not reintroduce workout ownership there.
4. Use the new adaptive-gold loader support to prove one doctrine-backed runtime path end-to-end before adding more bounded scheduler heuristics.
5. Continue shrinking remaining mixed-owner seams in `intelligence.py` only where they materially block coaching-state or SFR work outside the workout family.

Do not add broad new product features before steps 1-4 are complete.

## Latest Boundary Updates

- `apps/api/app/routers/workout.py`: `POST /workout/{workout_id}/log-set` session-state default seeding and persistable state-payload preparation now delegate to `core_engine.intelligence::{build_workout_session_state_defaults,prepare_workout_session_state_persistence_payload}`, while the router keeps only the ORM fetch/create/add wrapper around that reduction.
- `apps/api/app/routers/workout.py`: `GET /workout/{workout_id}/progress` now delegates payload shaping to `core_engine.intelligence::build_workout_progress_payload`.
- `apps/api/app/routers/workout.py`: `GET /workout/today` now delegates response hydration to `core_engine.intelligence::build_workout_today_payload` while keeping session selection and DB reads in the router.
- `apps/api/app/routers/workout.py`: `GET /workout/today` session selection now delegates the choose-resume/today/fallback policy to `core_engine.intelligence::resolve_workout_today_session_selection`.
- `apps/api/app/routers/workout.py`: `GET /workout/today` resume-state derivation now delegates latest logged-session and incomplete-session resolution to `core_engine.intelligence::resolve_latest_logged_workout_resume_state`.
- `apps/api/app/routers/workout.py`: planned session/exercise/program lookup now delegates stored payload traversal to `core_engine.intelligence::resolve_workout_plan_reference`.
- `apps/api/app/routers/profile.py`: weekly review status and default review-week windowing now delegate to `core_engine.intelligence::resolve_weekly_review_window` and `core_engine.intelligence::build_weekly_review_status_payload`.
- `apps/api/app/routers/profile.py`: previous-week review summary preparation now delegates raw plan/log normalization to `core_engine.intelligence::build_weekly_review_performance_summary`, so the router keeps only weekly-review SQL queries before response validation and persistence.
- `apps/api/app/routers/profile.py`: `GET /profile/training-state` now delegates canonical user-state assembly to `core_engine.user_state::build_user_training_state`, keeping the router limited to SQL reads and response validation.
- `apps/api/app/models.py`, `apps/api/app/schemas.py`, `apps/api/app/adaptive_schema.py`, `packages/core-engine/core_engine/user_state.py`, and `apps/api/app/routers/profile.py`: first-class coaching constraint state now persists and flows through the canonical runtime contract. `User` stores `session_time_budget_minutes`, `movement_restrictions`, and `near_failure_tolerance`; profile upsert/response schemas expose them; canonical `UserTrainingState` now includes `constraint_state`; and plan-decision training-state helpers consume the same normalized constraint payload.
- `apps/api/app/models.py`, `apps/api/app/schemas.py`, `apps/api/app/adaptive_schema.py`, `packages/core-engine/core_engine/user_state.py`, `packages/core-engine/core_engine/intelligence.py`, and `apps/api/app/routers/profile.py`: weekly check-in readiness fields now persist and flow through the canonical runtime contract. `WeeklyCheckin` stores `sleep_quality`, `stress_level`, and `pain_flags`; weekly-checkin persistence payload shaping now threads those fields; and canonical `UserTrainingState` now includes `readiness_state` with derived `recovery_risk_flags`.
- `packages/core-engine/core_engine/generation.py`, `packages/core-engine/core_engine/decision_coach_preview.py`, `packages/core-engine/core_engine/decision_progression.py`, and `apps/api/app/routers/plan.py`: coach-preview now carries canonical `readiness_state` into the readiness scorer when `readiness_score` is omitted from the request, and the decision trace now records whether readiness came from the request, context state, or raw request metrics only.
- `packages/core-engine/core_engine/decision_weekly_review.py` and `apps/api/app/routers/profile.py`: weekly-review submit now also consumes canonical `readiness_state` from recent check-ins, and the decision trace now records a readiness-state adjustment step with explicit source and matched penalty rules.
- `packages/core-engine/core_engine/decision_progression.py`, `packages/core-engine/core_engine/decision_coach_preview.py`, and `apps/api/tests/test_plan_intelligence_api.py`: the first deterministic `stimulus_fatigue_response` snapshot now exists in the progression family and is surfaced through coach-preview traces for underperformance/high-fatigue interpretation.
- `packages/core-engine/core_engine/decision_progression.py` and `packages/core-engine/core_engine/decision_weekly_review.py`: that SFR snapshot now drives real behavior in two families, triggering deload when recoverability is low and deload pressure is high, and forcing recovery-limited weekly-review guidance when recovery signals are poor.
- `packages/core-engine/core_engine/generation.py`, `packages/core-engine/core_engine/scheduler.py`, and `apps/api/tests/test_program_catalog_and_selection.py`: generated-week runtime now also derives an SFR snapshot from canonical readiness/fatigue/adherence inputs, uses it to trigger recovery-limited scheduler deloads, and surfaces structured `substitution_pressure` / `substitution_guidance` on planned exercises.
- `packages/core-engine/core_engine/decision_weekly_review.py` and `apps/api/tests/test_weekly_review.py`: weekly-review now uses the SFR snapshot to bound real adjustment payloads as well, applying recovery-limited global set/load adjustments and suppressing positive progression or weak-point boost overrides when recoverability is clearly constrained.
- `apps/api/app/routers/plan.py`: frequency adaptation preview/apply now build a canonical user-state payload through `core_engine.user_state::build_user_training_state` and feed it into `core_engine.generation::resolve_frequency_adaptation_request_context` instead of deriving week/recovery context ad hoc from router-local plan and soreness parsing.
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/coach-preview` now builds canonical user state and passes it into `core_engine.generation::build_coach_preview_context`, so preview history comes from `UserTrainingState` first rather than router-local `WorkoutSetLog` serialization.
- `packages/core-engine/core_engine/generation.py`: `resolve_week_generation_runtime_inputs` now prefers canonical user-state performance history, adherence, `fatigue_state.soreness_by_muscle`, and `generation_state.prior_generated_weeks_by_program` when provided, while preserving raw fallback inputs for compatibility.
- `packages/core-engine/core_engine/user_state.py` and `apps/api/app/adaptive_schema.py`: canonical user-training-state generation context now includes normalized `fatigue_state.soreness_by_muscle` plus `generation_state.prior_generated_weeks_by_program`, and `/profile/training-state` now emits those fields from persisted prior plans.
- `apps/api/app/routers/profile.py`: `GET /profile/program-recommendation` and `POST /profile/program-switch` now share `core_engine.decision_program_recommendation::{prepare_program_recommendation_runtime,prepare_program_switch_runtime}` through the stable package surface, so candidate resolution, deterministic selection, and switch preflight/apply gating are authoritative in the dedicated decision-family module while canonical user-state context still comes from `UserTrainingState`.
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/coach-preview` and `POST /plan/generate-week` now share `core_engine.generation::prepare_generation_template_runtime` for template loading/selection orchestration around `resolve_generation_template_choice`; the router keeps only HTTP error translation, SQL reads, and persistence around those flows.
- `apps/api/app/routers/plan.py`: `POST /plan/generate-week` now also centralizes its canonical training-state SQL fan-in and `resolve_week_generation_runtime_inputs` wiring through `_prepare_plan_generation_runtime`, so the route body no longer inlines the full generation-context read set.
- `apps/api/app/routers/workout.py`: `POST /workout/{workout_id}/log-set` now seeds first-exposure `ExerciseState.current_working_weight` through `core_engine.rules_runtime::resolve_starting_load`, using canonical `starting_load_rules` estimated-1RM fallback when the planned exercise carries `estimated_1rm`; the response now exposes the optional `starting_load_decision_trace` for that first-exposure path.
- `packages/core-engine/core_engine/intelligence.py`: progression and phase-transition logic now derive intro-week protection, fatigue RPE thresholds, scheduled-deload cadence, and early-deload trigger matching through `core_engine.rules_runtime::{resolve_adaptive_rule_runtime,evaluate_deload_signal}` rather than parsing rule payloads inline.
- `apps/api/app/routers/plan.py`: `POST /plan/generate-week` now passes the selected template rule set into `core_engine.scheduler::generate_week_plan`, so authored `substitution_rules` participate in generated-week equipment mismatch handling instead of the scheduler relying only on inline heuristics.
- `packages/core-engine/core_engine/scheduler.py`: planned exercise assembly now delegates equipment mismatch handling to `core_engine.rules_runtime::resolve_equipment_substitution`, preserves the compatible substitution list, and emits `substitution_decision_trace` on generated exercises.
- `apps/api/app/routers/workout.py`: `POST /workout/{workout_id}/log-set` now persists `ExerciseState.consecutive_under_target_exposures` / `last_progression_action`, resolves repeat-failure substitution recommendations through `core_engine.rules_runtime::resolve_repeat_failure_substitution`, and includes the optional structured `substitution_recommendation` inside live workout guidance.
- `apps/api/app/routers/workout.py`: `GET /workout/today` now rehydrates the same repeat-failure substitution recommendation from persisted `ExerciseState` plus planned exercise substitutions, so the guidance survives beyond the immediate log-set response.
- `packages/core-engine/core_engine/intelligence.py`: repeat-failure substitution payload shaping now delegates to `build_repeat_failure_substitution_payload`, and `build_workout_today_session_state_payloads` now prepares persisted workout-today session-state rows plus substitution guidance before final hydration.
- `packages/core-engine/core_engine/intelligence.py`: planned exercise normalization for `POST /workout/{workout_id}/log-set` now delegates to `resolve_workout_log_set_plan_context`, so rep-range coercion, planned-set fallback, and planned-weight fallback are no longer shaped inline in the router.
- `apps/api/app/routers/profile.py`: weekly review submit now delegates previous-week summary preparation, decision packaging, and response assembly to `core_engine.intelligence::{build_weekly_review_performance_summary,build_weekly_review_decision_payload,build_weekly_review_submit_payload}`.
- `apps/api/app/routers/workout.py`: `GET /workout/today` and `GET /workout/{workout_id}/progress` now delegate completed-set aggregation to `core_engine.intelligence::resolve_workout_completion_per_exercise`.
- `apps/api/app/routers/workout.py`: `GET /workout/{workout_id}/summary` now delegates workout-summary runtime preparation to `core_engine.intelligence::build_workout_performance_summary`, keeping only planned-session, workout-log, and bulk progression-state SQL reads in the router.
- `apps/api/app/routers/workout.py`: `GET /workout/today` now delegates persisted session-state count merging and live recommendation hydration to `core_engine.intelligence::build_workout_today_state_payloads`.
- `apps/api/app/routers/plan.py`: guide catalog/day/exercise endpoints now delegate display-name and payload shaping to `core_engine.generation::{format_program_display_name,build_guide_programs_payload,build_program_guide_payload,build_program_day_guide_payload,resolve_program_exercise_guide,build_program_exercise_guide_payload}`.
- `apps/api/app/routers/plan.py`: guide program-summary resolution now delegates to `core_engine.generation::resolve_program_guide_summary`, so guide not-found summary matching is no longer router-local.
- `apps/api/app/routers/plan.py`: coach-preview program display-name resolution now delegates to `core_engine.generation::resolve_program_display_name`, removing remaining router-local template-summary/fallback name normalization.
- `apps/api/app/routers/plan.py`: optional program rule-set loading now delegates to `core_engine.generation::resolve_optional_rule_set`, removing router-local linked-template rule-set resolution fallback logic from coach-preview and generate-week flows.
- `apps/api/app/routers/plan.py`: adaptation preview/apply onboarding-package linked-template resolution now delegates to `core_engine.generation::resolve_onboarding_program_id`, removing duplicated route-local linked-program ID mapping.
- `apps/api/app/routers/plan.py`: adaptation apply persistence state normalization now delegates to `core_engine.intelligence::build_frequency_adaptation_persistence_state`, removing route-local `dict(...)` copy shaping.
- `apps/api/app/routers/plan.py`: generate-week active-frequency-adaptation persistence payload normalization now delegates to `core_engine.intelligence::build_generated_week_adaptation_persistence_payload`.
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/coach-preview` now delegates preview response and persisted payload shaping to `core_engine.intelligence::build_coach_preview_payloads`, while `POST /plan/adaptation/apply` now delegates response shaping to `core_engine.intelligence::build_frequency_adaptation_apply_payload`.
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/apply-phase` and `POST /plan/intelligence/apply-specialization` now delegate recommendation-payload normalization, fallback next-phase handling, and applied-record field assembly to `core_engine.intelligence::{prepare_phase_apply_runtime,prepare_specialization_apply_runtime}`; the router keeps preview lookup, DB writes, and HTTP error translation only.
- `apps/api/app/routers/plan.py`: `POST /plan/generate-week` now delegates final returned-plan assembly to `core_engine.intelligence::build_generated_week_plan_payload`, including template/runtime trace attachment, weekly-review overlays, and active frequency-adaptation runtime application; the router keeps generation inputs, SQL reads, and persistence only.
- `apps/api/app/routers/plan.py`: `POST /plan/generate-week` now also delegates weekly-review overlay normalization (`adjustments`/`context` shaping) to `core_engine.intelligence::prepare_generated_week_review_overlay`, so `plan.py` keeps the review-cycle SQL read while deterministic overlay preparation lives in core-engine.
- `apps/api/app/routers/plan.py`: `POST /plan/adaptation/preview` and `POST /plan/adaptation/apply` now share `core_engine.generation::prepare_frequency_adaptation_runtime_inputs` for deterministic adaptation input shaping (resolved program/week/recovery context plus normalized request/profile payloads), while the router keeps SQL reads, onboarding package loading, and apply persistence.
- `packages/core-engine/core_engine/intelligence.py`: frequency-adaptation preview/apply decision traces now carry `request_runtime_trace` from the shared runtime-input helper, so adaptation recommendations preserve the upstream context-resolution trace instead of rebuilding rationale from partial route-local state.
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/coach-preview` now delegates request day-range normalization (`from_days`/`to_days` plus profile-day max selection) to `core_engine.generation::prepare_coach_preview_runtime_inputs`, and passes that runtime trace into `core_engine.intelligence::recommend_coach_intelligence_preview` so coach-preview decision traces preserve upstream request-shaping provenance.
- `apps/api/app/routers/plan.py`: coach-preview now also forwards template-selection runtime provenance into `recommend_coach_intelligence_preview`, so recommendation traces include both request-shaping and template-selection decision families end-to-end.
- `apps/api/app/routers/plan.py`: coach-preview recommendation-record field shaping now delegates to `core_engine.intelligence::build_coach_preview_recommendation_record_fields`, keeping deterministic persistence payload prep in core-engine while the router keeps DB writes.
- `apps/api/app/routers/plan.py`: apply-phase and apply-specialization now normalize source recommendation payload/fields via `core_engine.intelligence::prepare_coaching_apply_runtime_source` before interpreter runtime prep, reducing route-local normalization drift.
- `apps/api/app/routers/profile.py`: weekly-review submit week-window selection now delegates to `core_engine.intelligence::prepare_weekly_review_submit_window`, so default-vs-request week resolution and previous-week derivation are interpreter-owned.
- `apps/api/app/routers/profile.py`: weekly-review submit persistence payload shaping for `WeeklyReviewCycle.faults` and `WeeklyReviewCycle.adjustments` now delegates to `core_engine.intelligence::build_weekly_review_cycle_persistence_payload`, keeping deterministic storage payload normalization out of the router.
- `apps/api/app/routers/profile.py`: soreness create/update persistence payload shaping now delegates to `core_engine.intelligence::build_soreness_entry_persistence_payload`, removing remaining route-local severity-map normalization (`dict(...)`) for that decision family.
- `apps/api/app/routers/profile.py`: body-measurement create/update persistence payload shaping now delegates to `core_engine.intelligence::{build_body_measurement_create_payload,build_body_measurement_update_payload}`, removing route-local field patch normalization from measurement endpoints.
- `apps/api/app/routers/profile.py`: soreness/body-measurement list endpoint date-window runtime prep now delegates to `core_engine.intelligence::prepare_profile_date_window_runtime`, so shared start/end bound normalization no longer lives inline in route bodies.
- `apps/api/app/routers/profile.py`: profile upsert persistence-field shaping now delegates to `core_engine.intelligence::build_profile_upsert_persistence_payload`, so selected-program defaulting and payload normalization are interpreter-owned.
- `apps/api/app/routers/profile.py`: program recommendation/switch fallback input shaping now delegates to `core_engine.intelligence::prepare_profile_program_recommendation_inputs`, removing route-local current-program/day/split defaults and latest-plan payload coercion.
- `apps/api/app/routers/profile.py`: weekly-checkin response shaping now delegates to `core_engine.intelligence::build_weekly_checkin_response_payload`, removing route-local status/phase fallback payload construction.
- `apps/api/app/routers/profile.py`: weekly-checkin entry payload shaping now delegates to `core_engine.intelligence::build_weekly_checkin_persistence_payload`.
- `apps/api/app/routers/profile.py`: weekly-review user profile update payload shaping now delegates to `core_engine.intelligence::build_weekly_review_user_update_payload`.
- `apps/api/app/routers/profile.py`: weekly-review previous-week log window timestamp shaping now delegates to `core_engine.intelligence::prepare_weekly_review_log_window_runtime`.
- `apps/api/app/routers/workout.py`: `GET /workout/today` now delegates latest plan payload normalization (session list, session IDs, selected-program resolution, mesocycle/deload context) to `core_engine.intelligence::build_workout_today_plan_runtime`, keeping deterministic plan-context shaping in core-engine.
- `apps/api/app/routers/workout.py`: optional program rule-set loading now delegates to `core_engine.generation::resolve_optional_rule_set`, removing router-local linked-template rule-set resolution fallback logic from workout today/log-set/summary runtime prep.
- `apps/api/app/routers/workout.py`: log-set request runtime normalization now delegates to `core_engine.intelligence::prepare_workout_log_set_request_runtime`, centralizing primary-exercise fallback and normalized request field shaping before persistence and interpreter calls.
- `apps/api/app/routers/workout.py`: `GET /workout/today` now delegates workout-log projection runtime (resume `workout_id` rows and selected-session completion `exercise_id`/`set_index` rows) to `core_engine.intelligence::build_workout_today_log_runtime`, keeping deterministic log normalization in core-engine.
- `apps/api/app/routers/workout.py`: `GET /workout/{workout_id}/progress` now also delegates completion log projection (`exercise_id`/`set_index` rows) through `core_engine.intelligence::build_workout_today_log_runtime` before completed-set aggregation, removing remaining router-local row shaping in that route.
- `apps/api/app/routers/workout.py`: `GET /workout/{workout_id}/summary` now delegates primary-exercise-id normalization for progression-state lookup to `core_engine.intelligence::build_workout_summary_progression_lookup_runtime`, keeping deterministic summary lookup shaping out of the router.
- `apps/api/app/routers/workout.py`: `GET /workout/today` now also delegates progression lookup primary-exercise-id normalization from persisted session-state rows to `core_engine.intelligence::build_workout_today_progression_lookup_runtime`, so the route keeps only SQL reads before progression-state hydration.
- `apps/api/app/routers/workout.py`: `POST /workout/{workout_id}/log-set` now delegates final response payload assembly to `core_engine.intelligence::build_workout_log_set_payload`; the router keeps ORM writes, progression-state updates, and interpreter calls only.
- `apps/api/app/routers/plan.py`: `GET /plan/intelligence/recommendations` now delegates timeline-limit normalization and timeline-entry payload shaping to `core_engine.intelligence::{normalize_coaching_recommendation_timeline_limit,build_coaching_recommendation_timeline_payload}`, so the router keeps only auth, SQL read, and response validation.
- `apps/api/app/routers/plan.py`: `POST /plan/generate-week` now delegates `generate_week_plan` call-input normalization to `core_engine.generation::prepare_generate_week_plan_runtime_inputs`, so the route keeps SQL fan-in, template/rule loading, and persistence while deterministic request payload shaping lives in core-engine.
- `apps/api/app/routers/profile.py`: `GET /profile` now delegates profile-response defaulting and payload shaping to `core_engine.intelligence::build_profile_response_payload`, so the router keeps response validation while deterministic fallback normalization lives in core-engine.
- `apps/api/app/routers/workout.py`: workout plan-context resolution for `POST /workout/{workout_id}/log-set`, `GET /workout/{workout_id}/progress`, and `GET /workout/{workout_id}/summary` now delegates payload normalization and session/exercise/program resolution to `core_engine.intelligence::resolve_workout_plan_context`, so router-local deterministic payload coercion is removed while SQL reads remain in-router.
- `apps/api/app/routers/workout.py`: `GET /workout/today` latest-plan selection/payload normalization now delegates to `core_engine.intelligence::resolve_workout_today_plan_payload`, so router-local `plans[0].payload` coercion is removed while SQL reads and HTTP 404 mapping remain in-router.
- `packages/core-engine/core_engine/intelligence.py`: frequency adaptation preview/apply traces now follow a stricter shared interpreter contract (`version`, ordered `steps`, and machine-readable `reason_code`), and apply now reuses resolved preview context for weak-area/recovery fields.
- `packages/core-engine/core_engine/intelligence.py` + `packages/core-engine/core_engine/onboarding_adaptation.py`: frequency adaptation now consumes canonical `frequency_adaptation_rules.weak_area_bonus_slots` when building weak-area overlays and slot-priority scoring, reducing hardcoded weak-area pressure assumptions.
- `importers/pdf_doctrine_rules_v1.py`: doctrine distillation now extracts scheduled deload cadence (`deload every N weeks`) into typed `deload_rules.scheduled_every_n_weeks` when explicitly present in normalized source text.
- `importers/pdf_doctrine_rules_v1.py`: doctrine distillation now also extracts explicit repeat-failure substitution thresholds (`after N failed exposures`) into typed `substitution_rules.repeat_failure_trigger` values (numeric form), with default fallback retained when no explicit signal exists.
- `packages/core-engine/core_engine/rules_runtime.py` + tests: numeric repeat-failure trigger forms emitted by the distiller are now explicitly regression-covered for runtime threshold parsing.
- `importers/pdf_doctrine_rules_v1.py`: doctrine distillation now also extracts explicit early-deload trigger variants (`three consecutive under target sessions`) into typed `deload_rules.early_deload_trigger`, with canonical fallback retained when no explicit doctrine signal is present.
- `packages/core-engine/core_engine/rules_runtime.py` + tests: `three_consecutive_under_target_sessions` trigger behavior is explicitly regression-covered, including low-fatigue underperformance match semantics.
- `importers/pdf_doctrine_rules_v1.py`: doctrine distillation now extracts explicit fatigue-threshold trigger text (`session RPE avg >= X for N exposures`) into normalized typed condition strings in `fatigue_rules.high_fatigue_trigger.conditions`, with canonical fallback retained when no explicit doctrine signal is present.
- `importers/pdf_doctrine_rules_v1.py`: doctrine distillation now also grounds `fatigue_rules.high_fatigue_trigger` provenance from explicit fatigue-threshold excerpts when present, instead of relying only on generic intensity-shift excerpt matching.
- `packages/core-engine/core_engine/decision_frequency_adaptation.py`: started decision-family decomposition by moving frequency adaptation preview/apply and adaptation persistence payload builders into a dedicated module, while keeping router contracts and traces unchanged.
- `packages/core-engine/core_engine/decision_frequency_adaptation.py`: frequency-adaptation decomposition now also includes active adaptation runtime state resolution/application helpers used by generate-week (`resolve_active_frequency_adaptation_runtime`, `apply_active_frequency_adaptation_runtime`), further reducing direct ownership in `intelligence.py`.
- `packages/core-engine/core_engine/decision_weekly_review.py`: continued decision-family decomposition by moving weekly-review window/status/submit/persistence utility functions into a dedicated module, while keeping router contracts and observable API behavior unchanged.
- `packages/core-engine/core_engine/decision_weekly_review.py`: weekly-review decomposition now also includes decision interpretation and decision-payload shaping (`interpret_weekly_review_decision`, `build_weekly_review_decision_payload`), further reducing direct decision ownership in `intelligence.py`.
- `packages/core-engine/core_engine/intelligence.py`: duplicate weekly-review helper internals removed after delegation so decision ownership stays consolidated in `decision_weekly_review.py`.
- `packages/core-engine/core_engine/decision_weekly_review.py`: weekly-review decomposition now also includes plan-adjustment application helpers (`apply_weekly_review_adjustments_to_plan` and related helper chain), removing this remaining weekly-review transformation branch from `intelligence.py`.
- `packages/core-engine/core_engine/__init__.py`: weekly-review plan-adjustment exports are now also sourced from `decision_weekly_review.py`, keeping public module boundaries aligned with decision-family ownership.
- `packages/core-engine/core_engine/decision_coach_preview.py`: started coach-preview decomposition by moving response payload and recommendation-record-field shaping into a dedicated module, with `intelligence.py` delegating those payload helpers and public exports aligned via `__init__.py`.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coach-apply source/runtime prep internals now also live in this module, while `intelligence.py` preserves stable public wrapper signatures used by routers and delegates the low-level prep logic into the decision-family implementation.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coach-apply decision interpreters and applied-record payload builders (`interpret_coach_phase_apply_decision`, `interpret_coach_specialization_apply_decision`, `build_phase_applied_recommendation_record`, `build_specialization_applied_recommendation_record`) now also live in this module; `intelligence.py` wrappers preserve existing router-facing signatures.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coach-apply finalization payload shaping (`finalize_applied_coaching_recommendation`) now also lives in this module, with `intelligence.py` keeping the stable wrapper entrypoint used by plan apply routes.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coach-preview recommendation assembly internals now also live in this module (schedule/progression payload shaping, readiness fallback resolution, decision trace packaging, and `recommend_coach_intelligence_preview` orchestration); `intelligence.py` now delegates through a stable wrapper and injects existing decision dependencies.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coaching-recommendation timeline payload normalization now also lives in this module (`resolve_coaching_recommendation_rationale`, focus-muscle extraction, timeline entry builder, timeline payload builder, and timeline limit normalization); `intelligence.py` keeps stable wrappers used by plan recommendation timeline endpoints.
- `packages/core-engine/core_engine/user_state.py`: shared plan/profile training-state assembly now also lives in this module via `build_plan_decision_training_state` (canonical user-state wrapper with router-safe defaults for recent logs/checkins/reviews/prior plans and no direct exercise-state dependency); `apps/api/app/routers/plan.py` and `apps/api/app/routers/profile.py` now reuse that helper instead of route-local training-state payload shaping.
- `packages/core-engine/core_engine/decision_coach_preview.py`: apply response finalization wiring now also lives in this module via `build_applied_coaching_recommendation_response` (delegating deterministic trace/payload finalization to the same decision-family boundary); `apps/api/app/routers/plan.py` apply-phase and apply-specialization now share that helper instead of route-local finalize wiring.
- `packages/core-engine/core_engine/decision_coach_preview.py`: applied recommendation record-value shaping now also lives in this module via `build_applied_coaching_recommendation_record_values`; `apps/api/app/routers/plan.py` apply-phase and apply-specialization now reuse the shared constructor payload helper instead of route-local model-field wiring.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coach-preview runtime context assembly now also lives in this module via `prepare_coach_preview_decision_context` (canonical training-state + context payload prep with deterministic defaults and trace metadata); `apps/api/app/routers/plan.py` coach-preview now reuses that helper instead of route-local context-shaping logic.
- `packages/core-engine/core_engine/generation.py`: frequency-adaptation decision-runtime assembly now preserves top-level `context_trace` in `prepare_frequency_adaptation_decision_runtime`, restoring the expected trace contract used by generation-focused tests.
- `packages/core-engine/core_engine/generation.py`: plan generate-week runtime assembly now also lives in core-engine via `prepare_plan_generation_decision_runtime` (canonical training-state build + week-generation runtime input prep + structured trace packaging); `apps/api/app/routers/plan.py::_prepare_plan_generation_runtime` now keeps SQL fan-in only and delegates deterministic runtime payload construction to that helper.
- `packages/core-engine/core_engine/decision_weekly_review.py`: weekly-review performance summary decision family now also lives in this module via `summarize_weekly_review_performance` and `build_weekly_review_performance_summary` (planned-vs-performed aggregation, fault scoring, and structured summary trace packaging).
- `packages/core-engine/core_engine/intelligence.py`: weekly-review performance summary entrypoints now delegate to `decision_weekly_review` wrappers, further reducing direct weekly-review ownership in the intelligence module while preserving existing public signatures.
- `packages/core-engine/core_engine/__init__.py`: weekly-review performance summary exports are now sourced from `decision_weekly_review.py`, aligning public package boundaries with decision-family ownership.
- `packages/core-engine/core_engine/intelligence.py`: removed now-dead weekly-review summary helper internals (planned/performed aggregation + fault-scoring helper chain) after delegation, so weekly-review summary logic has a single authoritative owner in `decision_weekly_review.py`.
- `packages/core-engine/core_engine/decision_coach_preview.py`: apply confirm-path commit orchestration now also lives in this module via `prepare_applied_coaching_recommendation_commit_runtime` + `finalize_applied_coaching_recommendation_commit_runtime` (record insert payload shaping + post-flush recommendation/response payload finalization).
- `apps/api/app/routers/plan.py`: apply-phase/apply-specialization confirm paths now reuse that shared commit runtime helper pair; routes keep preview lookup, SQL writes/flush/commit, and HTTP error mapping only.
- `packages/core-engine/core_engine/decision_coach_preview.py`: apply decision-request runtime branch wiring now also lives in this module via `prepare_coaching_apply_decision_runtime` (normalized source-runtime extraction + phase/specialization branch argument shaping + trace metadata).
- `apps/api/app/routers/plan.py`: apply-phase/apply-specialization now delegate source-runtime branch argument wiring through `core_engine.intelligence::prepare_coaching_apply_decision_runtime`, so routes no longer duplicate deterministic source field unpacking before apply interpreters run.
- `packages/core-engine/core_engine/decision_coach_preview.py`: apply commit-runtime payload-key mapping by decision family now also lives in this module via `prepare_coaching_apply_commit_runtime`, so phase/specialization payload-key selection (`phase_transition` vs `specialization`) is no longer route-local.
- `apps/api/app/routers/plan.py`: apply-phase/apply-specialization commit paths now call `core_engine.intelligence::prepare_coaching_apply_commit_runtime` instead of hardcoding per-route payload-key wiring; DB writes/flush/commit remain in-router.
- `packages/core-engine/core_engine/decision_coach_preview.py`: apply route preflight/confirm branching runtime assembly now also lives in this module via `prepare_coaching_apply_route_runtime`, so the same helper owns response payload selection and optional commit-runtime preparation across decision kinds.
- `apps/api/app/routers/plan.py`: apply-phase/apply-specialization now share `core_engine.intelligence::prepare_coaching_apply_route_runtime` before response validation and DB persistence, removing remaining duplicated route-local preflight/confirm branching logic.
- `packages/core-engine/core_engine/decision_weekly_review.py`: weekly-review submit persistence value shaping now also lives in this module via `prepare_weekly_review_submit_persistence_values` (deterministic checkin/review ORM value payload prep + trace metadata from already-prepared summary and adjustment payloads).
- `apps/api/app/routers/profile.py`: `POST /weekly-review` now delegates checkin/review constructor value preparation to `prepare_weekly_review_submit_persistence_values`, keeping user/review DB writes in-router while removing route-local field wiring.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coach-preview commit runtime now also lives in this module via `prepare_coach_preview_commit_runtime` + `finalize_coach_preview_commit_runtime` (record insert payload shaping before flush, response/recommendation payload finalization after recommendation ID is known).
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/coach-preview` now delegates recommendation-record constructor payload prep and post-flush response payload finalization through those shared helpers, while retaining DB add/flush/commit in-router.
- `packages/core-engine/core_engine/intelligence.py`: workout log-set exercise-state progression runtime now also lives in this module via `prepare_workout_exercise_state_runtime` (existing-state normalization, first-exposure starting-load resolution, progression-state update payload prep, repeat-failure substitution payload shaping, and structured trace output).
- `apps/api/app/routers/workout.py`: `POST /workout/{workout_id}/log-set` now delegates exercise-state runtime preparation to `prepare_workout_exercise_state_runtime`; route keeps ORM read/create/add/commit only while deterministic state-field wiring is core-engine-owned.
- `packages/core-engine/core_engine/intelligence.py`: `prepare_workout_log_set_decision_runtime` now also emits deterministic exercise-state persistence payloads (`exercise_state_create_values`, `exercise_state_update_values`, `starting_load_runtime`, and `substitution_recommendation`) so router-local payload wiring is reduced further.
- `apps/api/app/routers/workout.py`: `POST /workout/{workout_id}/log-set` now persists `ExerciseState` using those pre-shaped core-engine payloads, keeping the route focused on ORM read/write + HTTP response validation.
- `packages/core-engine/core_engine/decision_frequency_adaptation.py`: adaptation preview/apply route orchestration now also lives behind `prepare_frequency_adaptation_route_runtime`, consolidating shared runtime-argument shaping, preview/apply interpreter dispatch, apply response payload shaping, and apply persistence-state shaping behind one decision-family helper.
- `apps/api/app/routers/plan.py`: `POST /plan/adaptation/preview` and `POST /plan/adaptation/apply` now both delegate deterministic adaptation route orchestration to `prepare_frequency_adaptation_route_runtime`; routes keep only profile guards, SQL fan-in, onboarding package load, persistence commit, and HTTP error mapping.
- `packages/core-engine/core_engine/decision_coach_preview.py`: coach-preview route composition now also lives behind `prepare_coach_preview_route_runtime`, consolidating preview interpreter dispatch + commit-runtime preparation into one decision-family helper with a structured trace.
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/coach-preview` now delegates deterministic preview/commit composition to `prepare_coach_preview_route_runtime`; route keeps only profile guards, SQL reads, template/rule loading, DB flush/commit, and HTTP error mapping.
- `packages/core-engine/core_engine/intelligence.py`: profile program-recommendation route composition now also lives behind `prepare_profile_program_recommendation_route_runtime`, consolidating recommendation-input fallback shaping and recommendation-runtime preparation in one deterministic helper.
- `apps/api/app/routers/profile.py`: `GET /profile/program-recommendation` and `POST /profile/program-switch` now both delegate shared deterministic recommendation runtime prep to `prepare_profile_program_recommendation_route_runtime`; routes keep SQL reads, switch persistence, and HTTP mapping.
- `packages/core-engine/core_engine/decision_weekly_review.py`: weekly-review submit route composition now also lives behind `prepare_weekly_review_submit_route_runtime`, consolidating decision payload, persistence payloads, user update payload, and submit response payload prep in one deterministic helper.
- `apps/api/app/routers/profile.py`: `POST /weekly-review` now delegates shared submit payload composition to `prepare_weekly_review_submit_route_runtime`; route keeps SQL reads, user/checkin/review persistence, and HTTP mapping.
- `packages/core-engine/core_engine/decision_coach_preview.py`: apply route finalization now also lives behind `prepare_coaching_apply_route_finalize_runtime`, consolidating preflight response passthrough vs confirm-path finalization (`applied_recommendation_id` + recommendation payload update) in one deterministic helper.
- `apps/api/app/routers/plan.py`: `POST /plan/intelligence/apply-phase` and `POST /plan/intelligence/apply-specialization` now delegate deterministic finalize branching to `prepare_coaching_apply_route_finalize_runtime`; routes keep recommendation lookup, DB add/flush/commit, and HTTP mapping.
- `packages/core-engine/core_engine/intelligence.py`: workout session-state upsert payload composition now also lives behind `prepare_workout_session_state_upsert_runtime`, consolidating default create-values seeding plus persisted update/live payload prep from in-session reduction.
- `apps/api/app/routers/workout.py`: `_upsert_workout_session_state` now delegates deterministic state create/update/live payload composition to `prepare_workout_session_state_upsert_runtime`; helper keeps only ORM fetch/create/add.
- `packages/core-engine/core_engine/decision_weekly_review.py`: weekly-review status and previous-week summary route composition now also live behind `prepare_weekly_review_status_route_runtime` and `prepare_weekly_review_summary_route_runtime`.
- `apps/api/app/routers/profile.py`: weekly-review status and summary composition now delegate to those helpers; route keeps date-window SQL reads plus HTTP response validation.
- `apps/api/app/routers/plan.py`: apply-phase and apply-specialization now share `_apply_coaching_decision_route` for common recommendation lookup, runtime dispatch, preflight-vs-confirm finalization, and optional applied-record persistence plumbing; deterministic apply interpretation/finalization remains core-engine-owned.
- `packages/core-engine/core_engine/decision_workout_session.py`: workout route-runtime composition now lives behind `prepare_workout_today_plan_route_runtime`, `prepare_workout_today_selection_route_runtime`, `prepare_workout_today_response_runtime`, `prepare_workout_progress_route_runtime`, and `prepare_workout_summary_route_runtime`, with structured traces for route-prep decisions.
- `apps/api/app/routers/workout.py`: `GET /workout/today`, `GET /workout/{workout_id}/progress`, and `GET /workout/{workout_id}/summary` now reuse those helpers; routers keep plan/log/session-state/progression-state SQL reads, optional rule-set loading, and HTTP mapping only.
- `packages/core-engine/tests/test_decision_workout_session.py`, `apps/api/tests/test_workout_resume.py`, `apps/api/tests/test_workout_progress.py`, `apps/api/tests/test_workout_summary.py`, and `apps/api/tests/test_workout_session_state.py`: focused regressions passed after the workout route-runtime extraction.
- `packages/core-engine/core_engine/generation.py`: generate-week route-runtime composition now also lives behind `prepare_generate_week_scheduler_runtime`, `prepare_generate_week_review_lookup_runtime`, and `prepare_generate_week_finalize_runtime`, centralizing scheduler kwargs, `week_start` review lookup parsing, finalized plan payload shaping, adaptation persistence shaping, and `WorkoutPlan` record values with structured traces.
- `apps/api/app/routers/plan.py`: `POST /plan/generate-week` now reuses those helpers instead of route-local scheduler argument assembly, `week_start` parsing, review overlay application, adaptation persistence shaping, and `WorkoutPlan` constructor field wiring.
- `packages/core-engine/tests/test_generation.py`, `apps/api/tests/test_program_catalog_and_selection.py`, `apps/api/tests/test_program_frequency_adaptation_api.py`, and `apps/api/tests/test_weekly_review.py`: focused regressions passed after the generate-week route-runtime extraction.
- `packages/core-engine/core_engine/decision_workout_session.py`: the workout decision-family now also owns `prepare_workout_log_set_context_route_runtime`, `prepare_workout_log_set_decision_route_runtime`, `prepare_workout_session_state_route_runtime`, `prepare_workout_log_set_response_runtime`, `prepare_workout_today_progression_route_runtime`, and `prepare_workout_summary_response_runtime`, consolidating the remaining deterministic `log-set`, `today`, and `summary` glue with structured traces.
- `apps/api/app/routers/workout.py`: `POST /workout/{workout_id}/log-set` now delegates request/context shaping, decision-runtime preparation, session-state upsert payload shaping, and final response payload shaping to the workout decision-family module; `GET /workout/today` now also delegates progression-id/rule-set prep there; `GET /workout/{workout_id}/summary` now also delegates summary response shaping there.
- `packages/core-engine/tests/test_decision_workout_session.py`, `apps/api/tests/test_workout_resume.py`, `apps/api/tests/test_workout_progress.py`, `apps/api/tests/test_workout_summary.py`, `apps/api/tests/test_workout_session_state.py`, and `apps/api/tests/test_workout_logset_feedback.py`: focused regressions passed after the completed workout-family extraction.
- `packages/core-engine/core_engine/decision_program_recommendation.py`: program recommendation/switch ownership now lives in a dedicated decision-family module (`resolve_program_recommendation_candidates`, `recommend_program_selection`, `prepare_program_recommendation_runtime`, `prepare_profile_program_recommendation_route_runtime`, `build_program_switch_payload`, `prepare_program_switch_runtime`), with `intelligence.py` retaining stable wrapper entrypoints and `__init__.py` exporting the new authoritative module surface.
- `packages/core-engine/tests/test_decision_program_recommendation.py`, `packages/core-engine/tests/test_intelligence.py`, and `apps/api/tests/test_program_recommendation_and_switch.py`: focused regressions passed after the program-recommendation decision-family extraction.
- `packages/core-engine/core_engine/decision_progression.py`: schedule adaptation, readiness scoring, progression action selection, progression rationale humanization, and phase-transition selection/humanization now live in a dedicated decision-family module; `intelligence.py` retains stable wrappers so coach-preview and other consumers keep the existing call surface.
- `packages/core-engine/tests/test_decision_progression.py`, `packages/core-engine/tests/test_intelligence.py`, and `apps/api/tests/test_plan_intelligence_api.py`: focused regressions passed after the progression/phase-transition decision-family extraction.
- `docs/current_state_decision_runtime_map.md`: current local-branch authority is now recorded in one operational map that names the authoritative module, canonical inputs/outputs, trace expectations, router responsibilities, and `intelligence.py` ownership status for each family.
- That extraction is now complete on the local branch; the next coding task is richer first-class coaching-state expansion, followed by deterministic stimulus-fatigue-response scoring.
- `apps/web/app/page.tsx`: home now behaves like a coaching dashboard instead of a status screen, with a coach brief, session blueprint, block radar, and momentum radar built from existing profile/today/history/review APIs.
- `apps/web/app/checkin/page.tsx`: weekly review now behaves like a coaching command center, surfacing readiness state, nutrition snapshot, prior-week lift audit severity, and humanized adaptive output instead of a plain form.
- `apps/web/app/week/page.tsx`: week plan now behaves like a planning cockpit, turning generated-week payloads into a command deck, mesocycle posture, coverage radar, session blueprint, adaptive review carryover, and frequency-adaptation runtime read built from `/plan/generate-week` and `/weekly-review/status`.
- `apps/web/app/today/page.tsx`: today now adds a pre-session intent card and a between-set coach surface on top of the existing workout runner, using deterministic workout, progress, and live-guidance payloads instead of client-only heuristics.
- `apps/web/app/history/page.tsx`: history now adds a progression brief, strength/bodyweight drift summary, and coach-queue framing on top of the existing analytics/timeline/calendar surfaces, keeping interpretation tied to `/history/analytics` and recommendation timeline payloads.
## Latest Slice

- Web coaching surfaces now expose authored-sequence completion more clearly:
  - coaching preview shows a `Program Transition` section when `phase_transition.transition_pending` is true
  - the section explains:
    - current block complete
    - rotate program
    - hold-last-authored-week fallback behavior
- This now appears on:
  - shared coaching panel surfaces (`Week`, `Check-In`, `Today`)
  - `Settings`, which still has its own preview UI
- Internal browser-testing docs now exist:
  - `docs/testing/INTERNAL_TESTER_RUNBOOK.md`
  - `docs/testing/INTERNAL_TEST_ISSUE_TEMPLATE.md`
