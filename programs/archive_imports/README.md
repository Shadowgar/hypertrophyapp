# Archived Imported Templates

This folder contains legacy `*_imported.json` artifacts generated from early ingestion passes.

Policy:
- These files are not used by runtime template selection.
- Runtime template catalog is sourced from canonical root templates (`full_body_v1.json`, `ppl_v1.json`, `upper_lower_v1.json`).
- Keep archived imports for audit/reference only.
