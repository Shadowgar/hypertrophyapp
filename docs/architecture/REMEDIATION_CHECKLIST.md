# Remediation Checklist

Active wave: `Blocker 1`

Priority order:

1. `ACTIVE` Make generated week emit one real top-level authoritative `decision_trace`, and remove fake trace stitching / missing-trace behavior on that path.
2. Move generated-week template selection into a named owner and keep `generation.py` orchestration-only on that path.
3. Strip doctrine invention out of `scheduler.py` on the generated-week spine.
4. Remove silent doctrine fallback from gold-path first-class generation behavior when canonical rule sets are absent.
5. Enforce explanation-law classification on first-class week surfaces so presentation does not invent rationale.
